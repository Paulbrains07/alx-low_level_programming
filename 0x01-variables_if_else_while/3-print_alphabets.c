#include <stdio.h>
/**
 * main - Entry point
 * Return: usually 0 (Good)
 */

int main(void)
{
	char p;

	char f;

	p = 'a';
	f = 'f';
	while
		(p <= 'z') {
			putchar(p);
			p++;
		}
	while
		(f <= 'Z') {
			putchar (f);
			f++;
		}
	putchar('\n');
	return (0);
}
