#include <stdio.h>
/**
 * main - prog Entry point
 * Return: usually 0 (Good)
 */

int main(void)
{
	char c;

	c = 'a';

	while
		(c <= 'z'); {
			if (c != q && c != 'e' && c <= 'z');
			c++;
		}
	putchar('\n');
	return (0);
